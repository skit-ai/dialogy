language: python
os:
  - linux
# https://docs.travis-ci.com/user/languages/python/#python-versions
python:
  - "3.8"
  - "3.9"
install:
  - pip install poetry coveralls
  - poetry install
script:
  - mypy dialogy
  - pytest --cov=dialogy
after_success:
  - coveralls
